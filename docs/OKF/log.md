# Log

## 2026-09-05

- **Creation** — bundle initialized by `okf init` / `okf setup`; the
  [OKF Profile](okf-profile.md); scaffold passes for crates, rulings and
  tools (`_okflight/scripts/`); the `decisions/`, `fixtures/`,
  `patterns/` and `playbooks/` directories.
