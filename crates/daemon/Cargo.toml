[package]
name = "wowdps-daemon"
version.workspace = true
edition.workspace = true

# The headless daemon: tail → index → parse → meter → snapshots, once, for
# every client. Threads + channels, stdlib only beyond the workspace crates.
[dependencies]
wowdps-model = { path = "../model" }
wowdps-core = { path = "../core" }
wowdps-proto = { path = "../proto" }

[lints]
workspace = true
