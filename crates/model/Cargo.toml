[package]
name = "wowdps-model"
version.workspace = true
edition.workspace = true

# Domain types only: no I/O, no parser, no dependencies. This is the crate
# boundary that makes "clients don't parse" enforceable by the compiler.
[dependencies]
